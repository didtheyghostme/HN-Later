# Block dependency lifecycle scripts by default. Rebuild trusted native packages explicitly if needed.
ignore-scripts=true

# npm 11.10+: wait this many days before accepting newly published package versions.
min-release-age=7
